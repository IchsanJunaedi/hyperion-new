// Stub for Next.js `server-only` package.
// In production, this prevents server-only modules from being imported in the browser.
// In Vitest (jsdom), we stub it to a no-op so tests can import server utilities.
export {};
